prétraitement des fichiers

fichier gtf qui renseigne l'utilisateur

plugin gtftk python traitement ( avec tous les chromosomes )
=> fichier texte avec tous les chromosomes ( problèmes chrom non conventionnels )

mettre des gtf de base

prétraitement :
- détecter l'espèce => correspondre les chrom
=> Proposer les fichiers
=> Voir si bed et gtf correspondent
=> ensemble pas de prefix chr 

enlever les prefix ?

- vérifier les tailles des chromosomes ( positions des peaks dépassent ? )
=> plugin avec c
- hook / C ? librarire

layout des appli ? 

template laravel ?

log à la fin de l'exécution : reproductibilité 
=> mode verbeux ? mode debug ? 

menu réduit options avancés

commencer par l'appli de base ( OLOGRAM )
retrieve GTF humain / chip atlas fichier bed
=> demander pour les bons fichiers à tester au début

=> test binomial ?

https : crypter les transferts
consentement : utilisations des données , cookies 
versionnement : github 

privé github ? 

réunions régulières 

conda : bionconda
mysql : phpmyadmin
php / apache

git flow / github flow

résultat final : pdf ? sortie graphique ?
=> dash python 

laravel : intégrer sortie html
=> glade

regle fichier controlleur ( MVC )
=> route
